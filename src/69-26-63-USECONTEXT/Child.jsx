import React from 'react';
import GradnChild from './GradnChild';

const Child = ({ isDarkMode, toggleTheme }) => {
  return (
    <div>
      <GradnChild />
    </div>
  );
};

export default Child;
